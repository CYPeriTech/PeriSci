# minimal_case (v0.2.x)

This folder is a **smoke-test fixture**, not a case asset.

- Input: `input.json` (authoritative config JSON text for v0.2)
- Expected: `expected.json` only asserts non-failure (no numerical guarantees in v0.2)

Formal case assets start in **v0.3.x** under `/cases/` (first: Poisson).
